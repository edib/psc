#!/bin/bash


vagrant snapshot save --force "m1" ready-m1
vagrant snapshot save --force "m2" ready-m2
vagrant snapshot save --force "m3" ready-m3
vagrant snapshot save --force "b1" ready-b1
vagrant snapshot save --force "b2" ready-b2
vagrant snapshot save --force "p1" ready-p1
vagrant snapshot save --force "p2" ready-p2


